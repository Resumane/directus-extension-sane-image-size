{
	"name": "directus-extension-sane-image-size",
	"description": "This extension modifies big images after upload to a preset maximum (width, height & quality).",
	"icon": "Image",
	"version": "1.2.0",
	"keywords": [
		"directus",
		"directus-extension",
		"directus-custom-hook",
		"image"
	],
	"license": "MIT",
	"type": "module",
	"files": [
		"dist",
		"assets/watermarks/*.svg",
		"README.md",
		"AUTHORS",
		"LICENSE"
	],
	"directus:extension": {
		"type": "hook",
		"path": "dist/index.js",
		"source": "src/index.js",
		"host": "^10.10.0"
	},
	"scripts": {
		"build": "directus-extension build",
		"dev": "directus-extension build -w --no-minify",
		"generate:watermarks": "node scripts/generate-watermark-wrappers.mjs",
		"link": "directus-extension link",
		"prebuild": "npm run generate:watermarks",
		"test": "node --test"
	},
	"devDependencies": {
		"@directus/extensions-sdk": "^11.0.7"
	}
}
